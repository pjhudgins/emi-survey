# Index card: argus @ 4a72b03a

Index `emi` v3.1 · run `20260903-0517` · profile `balanced-v2` · variant **stop** · membership **incomplete**  
Revision `4a72b03ac025836ca309c209969c508caba7bb8c` · history ? (? → ?, floor 2026-08-06) · ledger sha `6e03e9ac7bc1` · receipt sha `b3c080efc710`

## Stop card

Gate 3 evidence completeness failed. 108 of 408 findings carrying citations are unconfirmed (26.5%) against a 15% bound. 76 fail only on `key` locators naming git-derived facts that findings_standard_v1.1 defines no locator type for; six producers improvised the same unsupported convention, so the cause is the standard-and-tool interface, not false evidence. The other three Gate 3 criteria pass. No threshold was lowered: that needs a founder-authorised profile. Nothing is claimed about this repository.

### Membership placement

- clause 1: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.
- clause 2: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.
- clause 3: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.
- clause 4: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.
- clause 5: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.
- clause 6: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.
- clause 7: **undetermined** (none) — No assessor was run. The run stopped at Gate 3 before the assessment stage, so no binding placement exists. The Gate 1 triage recorded a non-binding P-evidence screen on a bounded manifest view; that screen is routing only and is deliberately not carried here.

